class Astar{

  constructor(parkingLot) {
    this.parkingLot = parkingLot;
  };

  search(startPos) {

  }

  neighbors(tile){
    var result = [];
    var x = tile.getX;
    var y = tile.getY;

    //West
  }

  updateMap(parkingLot) {
    this.parkingLot = parkingLot;
  }
}
